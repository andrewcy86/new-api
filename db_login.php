<?php
// Edit this information with the datbase login information
$db_host = $service['credentials']['host'];
$db_username = $service['credentials']['username'];
$db_password = $service['credentials']['password'];
$db_name = $service['credentials']['db_name'];
?>